import styled from 'styled-components';

export const StyledFormAlert = styled.div`
    display: flex;
    background-color: rgba(207, 44, 0, .1);
    border-radius: .5rem;
    padding: 1.5rem 1.2rem;
`;

export const IconBox = styled.div`
    margin-right: 1rem
`;

export const FormAlertTittle = styled.h5`
    font-size: 1.8rem;
    line-height: 2.5rem;
    font-weight: normal;
    color: #CF2C00;
`;

export const FormAlertText = styled.p`
    font-size: 1.2rem;
    line-height: 2rem;
    color: rgba(207, 44, 0, .5);
`;