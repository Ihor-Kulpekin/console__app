import React from 'react';
import {StyledTittleApp} from '../../styled-components/StyledTittleApp';

const TittleApp = ({className}) => (
  <StyledTittleApp className={className}>
    API - консолька
  </StyledTittleApp>
);

export default TittleApp;